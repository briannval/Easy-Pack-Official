"use client";
import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    brand: {
      100: "#f7fafc",
    },
  },
  fontWeights: {
    normal: 400,
    medium: 600,
    bold: 700,
  },
});
